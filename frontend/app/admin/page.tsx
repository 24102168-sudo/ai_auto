import { PageShell } from "@/components/page-shell";

export default function AdminPage() {
  return <PageShell title="Admin Panel"><p className="text-white/70">Monitor jobs, audit logs, queues, health, and tenant usage from a single command center.</p></PageShell>;
}
